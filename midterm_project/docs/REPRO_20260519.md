# 재현 기록 — 2026-05-19 일괄 재실행

본 문서는 `outputs/` 초기화 후 **`scripts/rerun_all_pipelines.py`** 로 V1~V16·JWT(25개)를 순차 실행한 결과를 정리한다.  
이전 `outputs`에 남아 있던 V15 부가 실험 산출물(`v15_custombias_*`, `v15_reportbias_*` 등)은 **현재 V15 코드와 불일치**하므로 삭제되었으며, 본 재현에서는 **`v15_summary.json` 단일 요약**만 생성된다.

---

## 1. 실행 조건

| 항목 | 값 |
|------|-----|
| 일시 | 2026-05-19 (로컬) |
| 스크립트 | `scripts/rerun_all_pipelines.py` |
| 총 소요 | 약 33분 (`batch_run_manifest.json`) |
| 성공/실패 | 25 / 0 |
| V15 프로필 | `defensive` (기본) |
| Optuna (배치 기본) | `V15_OPTUNA_TRIALS=12`, `V13_OPTUNA_TRIALS=24`, `V12_TURBO_OPTUNA_TRIALS=24` |
| 플롯 | V13·V14·V15·V16·Turbo는 `--no-plots` |
| 로그 | `outputs/batch_rerun.log` |

---

## 2. 데이터·품질 감사 (재실행)

`fusion_data_audit.py`: Train **270행**, Validation **100행** (sanitize 후, 행 삭제 없음).

`sensor_spatial_profiler.py uwb` (Train long-format):

| 단계 | 레코드 수 |
|------|-----------|
| Stage 0 (유효 UWB) | 1176 |
| Stage 1 Hard limits | 1149 |
| Stage 2 Var < 17 | 801 |
| Stage 3 MAD/앵커 | **707** |

Clean 풀: MAE **2.827 m**, Median **2.030 m**, P90 **6.827 m**.  
앵커별 평균(측정−진): **−0.81 ~ −3.64 m** (`sensor_profiler_uwb_clean_anchor_mean_error.csv`).

---

## 3. 검증 RMSE 요약 (`*_summary.json`)

동일 validation 세트. **튜닝 무결성**을 반드시 구분할 것.

### 3.1 최종·무결 튜닝 계열

| 파이프라인 | 대표 단계 | 검증 RMSE (m) | 튜닝 | 출처 |
|------------|-----------|---------------|------|------|
| **V15** | Fusion-HardGate | **1.462** | Train OOF 게이트 + Optuna (defensive) | `v15_summary.json` |
| V15 | Fusion-ReWeight | 1.475 | 동일 | 동일 |
| V15 | Wi-Fi-Guide | 1.574 | 동일 | 동일 |
| V13-fix | Step C | **1.416** | Train CV + Optuna | `v13_fix_summary.json` |
| V13 | Step C | 1.444 | Train CV + Optuna | `v13_summary.json` |
| V12 Turbo | Step C | 1.441 | Train CV + Optuna | `v12_turbo_summary.json` |
| V9 Strict | Step B | 1.624 | 게이트만 Train CV | `v9_strict_summary.json` |

### 3.2 검증 라벨 직접 튜닝 (비교 시 주의)

| 파이프라인 | Step C RMSE (m) | 비고 |
|------------|-----------------|------|
| V12 기본 | **1.445** | `objective`: validation RMSE 최소 |
| V11 | 1.555 | validation 그리드 |
| V10 Step D (KNN) | 1.739 | 검증 K=28 선택 |

### 3.3 실패·악화 사례

| 파이프라인 | RMSE (m) | 교훈 |
|------------|----------|------|
| V14 (JWT) | Step C **2.228** | 복잡 Wi-Fi 보정 + 융합 악화 |
| V16 (defensive, composite) | HardGate **1.827** | V15 대비 목적/분기 실험 — **최종 아님** |
| V8 Pure UWB | **7.110** | UWB 단독 불가 (`batch_rerun.log`) |
| V8 Hybrid | 3.508 | Wi-Fi 없이 융합 실패 |
| V1 전체 | **7.307** | 초기 융합 |

### 3.4 Pure Wi-Fi 바닥

V8 / V9 / V10 / V11 Step A: **1.553~1.574 m** (재실행 일치).

---

## 4. V15 재현 하이퍼파라미터 (defensive)

`outputs/v15_summary.json` (2026-05-19):

| 항목 | 값 |
|------|-----|
| Train CV Wi-Fi-Guide | 2.617 m |
| Train CV Fusion-ReWeight | 2.541 m |
| OOF 게이트 raw → 적용 | [1.09, 3.95] → **[1.15, 2.80] m** |
| 선택 gate_m | 2.536 m |
| uwb_variance_inflate | 14.33 |
| huber_f_fusion | 1.0 |
| IRLS thresh | 3.05 m |
| Wi-Fi 캘리브 | identity |
| wall_clock | 132.6 s |

---

## 5. 산출물 정책 (2026-05-19 이후)

- **재현 단일 진실원**: `v15_summary.json`, `batch_rerun.log`, 본 문서.
- **`run_reports/`**: 배치 재현 시 선택적 생성(`script_run_io` 사용 스크립트만). 대량 보관이 필요 없으면 `batch_rerun.log`로 대체 가능.
- **개발 중 실험 접두사**: `v15_custombias_*`, `v15_reportbias_*` 등은 **자동 재생성되지 않음** — V15 코드의 `--experiment` 플래그 등을 켠 경우에만 생성.

---

## 6. 관련 파일

- `outputs/batch_run_manifest.json`
- `docs/_metrics_snapshot_20260519.csv` (summary JSON 자동 추출)
- 팀 최종 명세: `docs/V15_PIPELINE_SPEC.md`
